using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Net;
using System.IO;

namespace JARVIS
{
    public static class WikiClient
    {
        /*
        public static string GetArticle(string title)
        {
            try
            {
                using (WebClient wc = new WebClient())
                {
                    wc.Encoding = Encoding.UTF8;

                    if (title.Contains(" "))
                        title = title.Replace(" ", "_");


                }
            }
            catch (Exception ex)
            {
                Speaker.Speak("Erro ao obter artigo da wikipédia.");
            }
            return string.Empty;
        }*/
    }
}
